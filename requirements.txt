# Core
ollama>=0.1.0
edge-tts>=6.1.0
rich>=13.0.0

# Voice input (Apple Silicon optimized)
mlx-whisper>=0.4.0
# silero-vad loaded via torch.hub (avoids onnxruntime dependency)

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0

# Image generation (local)
diffusers>=0.25.0
accelerate>=0.25.0
huggingface-hub>=0.20.0
Pillow>=10.0.0
term-image>=0.7.0

# Utilities
httpx>=0.25.0
